/**
 * Node.jsx is no longer used.
 * Enemy logic and rendering are now handled inside GameCanvas.jsx
 * as the Enemy component in the tunnel rail-shooter game.
 * This file is kept as a stub to avoid import errors if anything references it.
 */
export default () => null;
